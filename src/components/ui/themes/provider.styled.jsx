import styled from 'styled-components';

export const Footer = styled.footer`
  position: absolute;
    right: 0;
    bottom: 1%;
    left: 0;
  display: flex;
  justify-content: space-between;
  margin: 0 1%;
  font-size: 1.1rem;
  font-weight: bold;
`;